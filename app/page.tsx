export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: 40, maxWidth: 640 }}>
      <h1>United Stables — U Brand MCP</h1>
      <p>MCP endpoint: <code>/api/mcp</code> (Streamable HTTP, POST).</p>
      <p>Add this URL as a custom connector in Claude to apply $U brand guidelines.</p>
    </main>
  );
}
